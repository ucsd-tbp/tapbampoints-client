import React from 'react';

const StatisticsPage = () => (
  <div>
    <h3>Statistics</h3>
    <p className="understated">This page is still under construction! When it's done, it'll show how many members signed up, attended events (members vs. initiates), and other useful info.</p>
  </div>
);

export default StatisticsPage;
